name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag casualsimulation/rpi3-mongodb3:$(date +%s) --tag casualsimulation/rpi3-mongodb3:latest
